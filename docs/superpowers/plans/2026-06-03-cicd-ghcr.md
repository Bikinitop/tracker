# CI/CD: Docker Release to GHCR — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add CI (race-tested build gate on PRs/main) and a tag-driven release pipeline that publishes a multi-arch Docker image of the tracker to `ghcr.io/bikinitop/tracker`.

**Architecture:** A reusable `go-test.yml` workflow defines "the code passes" once; `ci.yml` (PR/push) and `release.yml` (tag `v*`) both call it. The release's publish job builds a multi-stage **distroless** image for **amd64+arm64** with buildx and pushes it to GHCR using the built-in `GITHUB_TOKEN`. The git tag is stamped into the binary via `-ldflags`.

**Tech Stack:** Docker multi-stage build, `gcr.io/distroless/static:nonroot`, GitHub Actions (`actions/setup-go`, `docker/setup-buildx-action`, `docker/setup-qemu-action`, `docker/login-action`, `docker/metadata-action`, `docker/build-push-action`), Go 1.25.

**Design doc:** `docs/superpowers/specs/2026-06-03-cicd-ghcr-design.md`

**Conventions:**
- Module path is `github.com/bikinitop/tracker`; entrypoint `./cmd/tracker`.
- Image name is lowercase `ghcr.io/bikinitop/tracker` (GHCR rejects uppercase).
- Branch for this work: `feat/cicd-ghcr` (already created).
- A local Docker daemon is needed for the Dockerfile verification steps (Tasks 3). If Docker is unavailable locally, note it and rely on the CI run in Task 5 / the release path as the live test — do not skip writing the files.

---

## File structure

```
cmd/tracker/main.go              # MODIFY: add `var version`, stamp into startup log
cmd/tracker/main_test.go         # MODIFY: assert version default
.dockerignore                    # CREATE
Dockerfile                       # CREATE: multi-stage → distroless static
.github/workflows/go-test.yml    # CREATE: reusable build+vet+race test
.github/workflows/ci.yml         # CREATE: PR + push to main → calls go-test.yml
.github/workflows/release.yml    # CREATE: tag v* → test + publish to GHCR
docs/RELEASING.md                # CREATE: release runbook (tagging + make-public step)
```

---

## Task 1: Version stamping in the binary

**Files:**
- Modify: `cmd/tracker/main.go`
- Modify: `cmd/tracker/main_test.go`

- [ ] **Step 1: Write the failing test**

Append to `cmd/tracker/main_test.go`:

```go
func TestVersionDefault(t *testing.T) {
	if version != "dev" {
		t.Errorf("expected default version %q, got %q", "dev", version)
	}
}
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `go test ./cmd/tracker/ -run TestVersionDefault`
Expected: FAIL — `undefined: version`.

- [ ] **Step 3: Add the version variable and stamp it into the startup log**

In `cmd/tracker/main.go`, add a package-level variable just after the import block (before `type server struct`):

```go
// version is the build version, overridden at release build time via
// -ldflags "-X main.version=<tag>". Defaults to "dev" for local builds.
var version = "dev"
```

Then change the startup log line (currently `log.Printf("Starting tracker server on %s", srv.addr)`) to:

```go
	log.Printf("tracker version %s starting on %s", version, srv.addr)
```

- [ ] **Step 4: Run the tests to verify they pass**

Run: `go test -race ./cmd/tracker/`
Expected: PASS (existing tests + `TestVersionDefault`).

- [ ] **Step 5: Verify the ldflags injection works**

Run: `go run -ldflags "-X main.version=v9.9.9" ./cmd/tracker 2>&1 | head -1` then Ctrl-C, OR build and run:
`go build -ldflags "-X main.version=v9.9.9" -o /tmp/tracker ./cmd/tracker && NATS_URL=disabled TRACKER_PORT=18080 /tmp/tracker &` then `sleep 1 && curl -s localhost:18080/health && kill %1`
Expected: the log shows `tracker version v9.9.9 starting on :18080`, and `/health` returns `ok`.

- [ ] **Step 6: Commit**

```bash
git add cmd/tracker/main.go cmd/tracker/main_test.go
git commit -m "feat: stamp build version into binary and startup log"
```

---

## Task 2: `.dockerignore`

**Files:**
- Create: `.dockerignore`

- [ ] **Step 1: Create `.dockerignore`**

```
.git
.github
docs
*.md
.dockerignore
Dockerfile
```

(This keeps the build context small. Only Go sources, `go.mod`, and `go.sum` are needed by the build.)

- [ ] **Step 2: Commit**

```bash
git add .dockerignore
git commit -m "build: add .dockerignore"
```

---

## Task 3: Dockerfile (multi-stage, distroless, cross-compiled)

**Files:**
- Create: `Dockerfile`

- [ ] **Step 1: Create `Dockerfile`**

```dockerfile
# syntax=docker/dockerfile:1

# ---- Builder ----
FROM --platform=$BUILDPLATFORM golang:1.25 AS builder

# buildx provides these automatically; declare them to use in the build.
ARG TARGETOS
ARG TARGETARCH
ARG VERSION=dev

WORKDIR /src

# Cached dependency layer.
COPY go.mod go.sum ./
RUN go mod download

# Build the static binary. Go cross-compiles natively via GOOS/GOARCH, so the
# arm64 build does not need QEMU emulation for the compile step.
COPY . .
RUN CGO_ENABLED=0 GOOS=$TARGETOS GOARCH=$TARGETARCH \
    go build -trimpath -ldflags "-s -w -X main.version=${VERSION}" \
    -o /tracker ./cmd/tracker

# ---- Runtime ----
FROM gcr.io/distroless/static:nonroot

COPY --from=builder /tracker /tracker

EXPOSE 8080
USER nonroot:nonroot
ENTRYPOINT ["/tracker"]
```

- [ ] **Step 2: Build the image locally**

Run: `docker build --build-arg VERSION=test -t tracker:test .`
Expected: build succeeds; final image is based on distroless. (If no local Docker daemon, record that and proceed — CI/release will exercise it.)

- [ ] **Step 3: Run the image and verify it serves**

Run:
```bash
docker run --rm -d --name tracker-test -p 18080:8080 -e NATS_URL=disabled tracker:test
sleep 1
curl -s localhost:18080/health; echo
docker logs tracker-test | head -1
docker rm -f tracker-test
```
Expected: `/health` prints `ok`; logs show `tracker version test starting on :8080`.

- [ ] **Step 4: Commit**

```bash
git add Dockerfile
git commit -m "build: multi-stage distroless Dockerfile with version build-arg"
```

---

## Task 4: Reusable test workflow

**Files:**
- Create: `.github/workflows/go-test.yml`

- [ ] **Step 1: Create `.github/workflows/go-test.yml`**

```yaml
name: Go Test

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Test (race detector)
        run: go test -race ./...
```

- [ ] **Step 2: Validate YAML locally (optional)**

Run: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/go-test.yml')); print('valid')"`
Expected: `valid`. (Full validation happens when CI runs in Task 5.)

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/go-test.yml
git commit -m "ci: reusable go-test workflow (build, vet, race)"
```

---

## Task 5: CI workflow (PR + push to main)

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: Create `.github/workflows/ci.yml`**

```yaml
name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    uses: ./.github/workflows/go-test.yml
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: run go-test on pull requests and pushes to main"
```

- [ ] **Step 3: Push the branch, open the PR, and verify CI runs green**

`ci.yml` triggers on `pull_request` (and pushes to `main`), so it only runs once a PR exists for this branch. Push and open the PR:

```bash
git push -u origin feat/cicd-ghcr
gh pr create --base main --head feat/cicd-ghcr \
  --title "ci: Docker release to GHCR + CI test gate" \
  --body "Adds reusable go-test workflow, CI on PRs/main, a multi-stage distroless Dockerfile, and a tag-driven multi-arch release to ghcr.io/bikinitop/tracker. See docs/superpowers/specs/2026-06-03-cicd-ghcr-design.md."
```

Then watch the CI run the PR triggered:

```bash
RUN=$(gh run list --workflow=CI --branch feat/cicd-ghcr --limit 1 --json databaseId --jq '.[0].databaseId')
gh run watch "$RUN" --exit-status
```

Expected: the `test` job passes (build, vet, `go test -race ./...` all green). This proves the reusable workflow wiring works. (This is also the PR used at finish; no second PR is needed.)

---

## Task 6: Release workflow (tag `v*` → test + publish to GHCR)

**Files:**
- Create: `.github/workflows/release.yml`

- [ ] **Step 1: Create `.github/workflows/release.yml`**

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  test:
    uses: ./.github/workflows/go-test.yml

  publish:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/bikinitop/tracker
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            VERSION=${{ github.ref_name }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
```

- [ ] **Step 2: Validate YAML locally (optional)**

Run: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml')); print('valid')"`
Expected: `valid`.

- [ ] **Step 3: Commit and push**

```bash
git add .github/workflows/release.yml
git commit -m "ci: release workflow publishing multi-arch image to GHCR on v* tags"
git push
```

(The release workflow is exercised by a real tag after this branch merges — see RELEASING.md. Do **not** push a tag from the feature branch; tags should be cut from `main` after merge.)

---

## Task 7: Release runbook

**Files:**
- Create: `docs/RELEASING.md`

- [ ] **Step 1: Create `docs/RELEASING.md`**

```markdown
# Releasing

The tracker is released as a multi-arch Docker image to GitHub Container
Registry (GHCR) at `ghcr.io/bikinitop/tracker`.

## Cutting a release

1. Ensure `main` is green (CI passes).
2. Tag a semver version on `main` and push the tag:
   ```bash
   git checkout main && git pull
   git tag v1.0.0
   git push origin v1.0.0
   ```
3. The `Release` workflow runs the test suite, then builds and pushes:
   - `ghcr.io/bikinitop/tracker:1.0.0`
   - `ghcr.io/bikinitop/tracker:1.0`
   - `ghcr.io/bikinitop/tracker:1`
   - `ghcr.io/bikinitop/tracker:latest`
   for `linux/amd64` and `linux/arm64`.

## Make the package public (one-time, manual)

GHCR publishes a new package as **private**, and `GITHUB_TOKEN` cannot change
visibility. After the first release:

1. Go to GitHub → the `bikinitop` org/user → **Packages** → `tracker`.
2. **Package settings** → **Danger Zone** → **Change visibility** → **Public**.

Thereafter anyone can `docker pull ghcr.io/bikinitop/tracker:<tag>` without auth.

## Running the image

```bash
docker run --rm -p 8080:8080 \
  -e NATS_URL=nats://your-nats:4222 \
  ghcr.io/bikinitop/tracker:latest
```

The container logs its build version on startup
(`tracker version v1.0.0 starting on :8080`).
```

- [ ] **Step 2: Commit and push**

```bash
git add docs/RELEASING.md
git commit -m "docs: add release runbook (tagging + make-public step)"
git push
```

---

## Final verification

- [ ] **CI is green on the branch**

Run: `gh run list --workflow=CI --branch feat/cicd-ghcr --limit 1`
Expected: latest run `completed / success`.

- [ ] **Workflows are syntactically valid and discovered by GitHub**

Run: `gh workflow list`
Expected: `CI`, `Release`, and `Go Test` appear in the list.

- [ ] **Go suite still green**

Run: `go build ./... && go vet ./... && go test -race ./...`
Expected: all packages pass.

---

## Self-review notes (spec coverage)

- CI on PR/push with race test → Tasks 4, 5.
- Release on `v*` tag, test-gated → Task 6 (`needs: test`).
- Reusable workflow (single source of truth) → Task 4, called by Tasks 5 & 6.
- Multi-stage distroless image → Task 3.
- Multi-arch amd64+arm64 (buildx + QEMU, native cross-compile) → Tasks 3, 6.
- Semver + latest tags via metadata-action → Task 6.
- Version stamping via `-ldflags`, logged at startup → Tasks 1, 3, 6.
- GHCR auth via `GITHUB_TOKEN` + `packages: write` → Task 6.
- Public-visibility manual step documented → Task 7.
- `.dockerignore` → Task 2.
